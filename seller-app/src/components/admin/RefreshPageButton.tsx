/**
 * Botón compacto para recargar la página actual.
 */
import RefreshButton from '@/components/ui/RefreshButton'
/** Botón compacto para recargar la página. */
export default function RefreshPageButton() {
  return <RefreshButton compact />
}
